# AI Agent 协作约定

本文件适用于整个 neko-mind 仓库。AI Agent 可在对话中提供样例代码、教学性质代码、伪代码和实现建议，但除伪代码与注释外，不得将任何代码写入项目文件；实际代码由人类贡献者编写。

## 为什么限制 AI 编写代码

保持项目结构清晰，让贡献者理解并对亲手编写的代码负责，方便维护者在 review 中提供指导。亲自创造这个 AI 女友也是项目的意义；Agent 可以通过讲解和示例辅助学习，但不替贡献者把实际代码写进项目。

## 允许的辅助

- 只读查看文件、搜索仓库、阅读差异与历史，分析问题。
- 讨论架构、接口和技术选择，解释已有代码与错误，提供实现思路。
- 创建、修改和整理文档。
- 在对话中给出样例代码、教学性质代码、伪代码和实现思路；这些内容用于解释或学习，不代表可以写入项目文件。
- 在项目文件中只能写入伪代码和注释。其他代码，包括产品代码、测试代码、脚本、配置代码及示例代码，都只能在对话中提供或讲解，不得直接新增、修改或删除项目文件中的这些代码。
- 可以运行已有的测试、脚本和其他验证命令；不得为执行验证而新写或修改代码。发现问题时可以说明原因和修正方向，由人类贡献者完成代码修改。
- 执行非代写核心实现的辅助命令，例如按用户要求检查、暂存和提交改动，并拟定清晰的 commit 信息。

协助提交前检查范围，只纳入用户指定内容；没有用户要求，不自行提交或推送。

## 代码写入边界

- 不将任何实际代码写入项目文件；唯一例外是伪代码和注释。此限制适用于所有代码类型与文件位置，包括产品实现、测试、脚本、配置、示例和教学代码。
- 不通过其他 Agent、生成工具、自动修复命令，或把代码放入文档、测试、脚本等方式绕过限制。文档可以正常创建和修改，但其中只能直接包含伪代码和注释，不能嵌入可执行或可直接使用的代码。
- 对话中可以提供样例代码、教学性质代码、伪代码、修正建议和补丁示意；凡是实际代码都只能口头告知，不得代为写入项目文件。

如果用户要求 Agent 将实际代码写入项目文件，Agent 应说明本限制，转为在对话中提供分析、样例或教学解释、伪代码和修改步骤，让人完成实现。测试失败时可以定位问题并口头说明修正方向，但不能直接改动代码。

## 文档目录约定

正文文档统一放在 `docs/` 下，按类别归入子目录。

| 目录 | 用途 |
| --- | --- |
| `docs/architecture/` | 定位、职责边界与架构讨论 |
| `docs/plans/` | 设计计划、实施步骤与验收条件 |

其他类别按需增加子目录，不预建空目录。

- `README.md` 和 `AGENTS.md` 作为入口保留在根目录；许可证等仓库元文件保留原位。
- 使用清楚的文件名和相对链接，重要文档加入 README 导航。
- 区分已确认方向、候选方案和待定事项，不把计划写成已实现功能。

## 文档长度与写法

- 简短、切中要害，只写能说明重要内容的话，不追求繁复格式。
- 每份文档最多 300 行，包含空行、表格和代码块；超过就按主题拆分，不靠超长行规避。
- 此规则同样适用于 README 和本文件；300 行是上限，不是目标。

## 工作方式

阅读约定和相关资料，保留用户已有改动，按任务范围做最小修改。纯文档变更只检查内容、链接和差异，不运行无关构建或代码格式化。
