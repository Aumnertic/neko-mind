# AI Agent 协作约定

本文件适用于整个 neko-mind 仓库。AI Agent 只提供必要辅助；产品功能与核心逻辑由人类贡献者编写。测试代码和项目辅助脚本是允许由 Agent 编写的例外。

## 为什么限制 AI 编写代码

保持项目结构清晰，让贡献者理解并对亲手编写的代码负责，方便维护者在 review 中提供指导。亲自创造这个 AI 女友也是项目的意义；测试和脚本交给 Agent 辅助，让人专注于核心实现。

## 允许的辅助

- 只读查看文件、搜索仓库、阅读差异与历史，分析问题。
- 讨论架构、接口和技术选择，解释已有代码与错误，提供实现思路。
- 创建、修改和整理文档。
- 编写和维护测试文件、测试代码及项目辅助脚本，执行相关验证。修改内联测试时仅限测试部分，不改动产品逻辑。
- 执行非代写核心实现的辅助命令，例如按用户要求检查、暂存和提交改动，并拟定清晰的 commit 信息。

协助提交前检查范围，只纳入用户指定内容；没有用户要求，不自行提交或推送。

## 核心实现的边界

- 不编写、修改或删除产品功能与核心逻辑，也不代为修复、重构或格式化这些代码。
- 不通过其他 Agent、生成工具、自动修复命令，或把业务实现放进测试和脚本等方式绕过限制。
- 聊天和文档中也不提供可直接使用的核心实现或补丁；可以用字段、接口名称、流程和图示说明设计。测试与辅助脚本不受此项代写限制。

如果用户要求 Agent 代写核心实现，Agent 有责任说明本限制，转为提供分析、实现步骤或测试辅助，让人完成实现。测试失败时可以定位问题，但不能顺手修复产品代码。

## 文档目录约定

正文文档统一放在 `docs/` 下，按类别归入子目录。

| 目录 | 用途 |
| --- | --- |
| `docs/architecture/` | 定位、职责边界与架构讨论 |
| `docs/plans/` | 设计计划、实施步骤与验收条件 |

其他类别按需增加子目录，不预建空目录。

- `README.md` 和 `AGENTS.md` 作为入口保留在根目录；许可证等仓库元文件保留原位。
- 使用清楚的文件名和相对链接，重要文档加入 README 导航。
- 区分已确认方向、候选方案和待定事项，不把计划写成已实现功能。

## 文档长度与写法

- 简短、切中要害，只写能说明重要内容的话，不追求繁复格式。
- 每份文档最多 300 行，包含空行、表格和代码块；超过就按主题拆分，不靠超长行规避。
- 此规则同样适用于 README 和本文件；300 行是上限，不是目标。

## 工作方式

阅读约定和相关资料，保留用户已有改动，按任务范围做最小修改。纯文档变更只检查内容、链接和差异，不运行无关构建或代码格式化。
